<Window x:Class="LoginPage.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Height="300" Width="400" Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="LOGIN" Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Center"/>

        <Label Content="Username:" Grid.Row="1" Grid.Column="0" />
        <TextBox Grid.Row="1" Grid.Column="1" BorderBrush="Black" BorderThickness="1"/>

        <Label Content="Password:" Grid.Row="2" Grid.Column="0" />
        <TextBox Grid.Row="2" Grid.Column="1" BorderBrush="Black" BorderThickness="1"/>

        <Button Content="Login" Grid.Row="3" HorizontalAlignment="Left" Background="Black" Foreground="White" Padding="5,2" Margin="91,10,0,10" Grid.Column="1"/>
    </Grid>
</Window>
